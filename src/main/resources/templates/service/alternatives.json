{
  "${project.artifactId}": {
    "destination": "/usr/lib/flume-ng/plugins.d/${project.artifactId}",
    "source": "${project.artifactId}",
    "priority": 10,
    "isDirectory": true
  }
}
