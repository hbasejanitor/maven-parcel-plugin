{
  "schema_version": 1,
  "name": "HBASE10_PROXY_FLUME_SOURCE",
  "version": "${project.version}-${cdh.version}",
  "depends": "CDH (>= 5.2), CDH (<< 6.0)",
  "setActiveSymlink": true,
  "packages": [
    {
      "name": "hbase10-proxy-flume-source",
      "version": "${project.version}"
    }
  ],

  "components": [
    {
      "name": "hbase10-proxy-flume-source",
      "version": "${project.version}",
      "pkg_version": "${project.version}",
      "pkg_release": "${project.version}"
    }
  ],

  "provides": [
    "flume-plugin"
  ],

  "groups": [],

  "scripts": {
    "defines": "hbase10-proxy-flume-source-env.sh"
  },

  "users": {
  }
}
